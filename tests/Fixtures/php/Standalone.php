<?php

declare(strict_types=1);

/**
 * Declared outside any namespace, so its FQCN is the bare class name and the
 * reader finds the declaration at the top level rather than inside a
 * namespace block.
 */
class Standalone
{
    public const ONLY = 'only';
}
